vidrioroto@[]vidriosrotos;
class vidrioroto{
	int x, y, quantoscavar=1, quantosvidrio=1;
	string map;
	vidrioroto(int mx, int my, string mmap){
		x=mx;
		y=my;
		map=mmap;
	}
}
void colocar_vidrioroto(int x, int y, string map){
	vidrioroto o(x, y, map);
	vidriosrotos.insert_last(o);
}
