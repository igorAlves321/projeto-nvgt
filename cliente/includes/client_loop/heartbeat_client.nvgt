// Heartbeat do cliente para manter a sessao ativa no servidor.

void send_client_heartbeat_if_needed() {
	if(heartbeat_timer.elapsed < HEARTBEAT_INTERVAL) return;
	heartbeat_timer.restart();
	send_unreliable(peer_id, "heartbeat", 0);
}

